{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Kaiser: Debug Build",
            "type": "shell",
            "command": "${workspaceFolder}/Tools/nodejs_bin/node-linux64 ${workspaceFolder}/make.js --debug --compile",
            "problemMatcher":"$gcc",
            "group":{
                "kind": "build",
                "isDefault": true
            }
            
        }
    ]
}